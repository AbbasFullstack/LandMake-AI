import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { trpc } from "@/lib/trpc";
import { ArrowUpRight, Clock3, Download, FileCode2, Plus, WandSparkles } from "lucide-react";
import { FormEvent, useState } from "react";
import { toast } from "sonner";
import { useLocation } from "wouter";

export default function Home() {
  const [, setLocation] = useLocation();
  const utils = trpc.useUtils();
  const { data, isLoading } = trpc.dashboard.overview.useQuery();
  const [name, setName] = useState("My landing page");
  const [prompt, setPrompt] = useState("Build a dark SaaS landing page for remote teams with features, pricing, and a clear demo CTA.");
  const createProject = trpc.projects.create.useMutation({
    onSuccess: ({ project }) => { utils.dashboard.overview.invalidate(); toast.success("Landing page generated"); setLocation(`/projects/${project.id}`); },
    onError: error => toast.error(error.message),
  });
  const submit = (event: FormEvent) => { event.preventDefault(); createProject.mutate({ name, prompt }); };
  return <div className="mx-auto max-w-[1440px] space-y-7 font-[Manrope,sans-serif] text-slate-950"><section className="relative overflow-hidden rounded-[30px] border border-violet-100 bg-white px-6 py-7 shadow-[0_18px_60px_rgba(76,29,149,.08)] sm:px-8"><div className="pointer-events-none absolute -right-20 -top-24 h-64 w-64 rounded-full bg-violet-200/50 blur-3xl" /><div className="relative grid gap-8 lg:grid-cols-[1.1fr_.9fr] lg:items-end"><div><div className="flex items-center gap-2 text-xs font-bold uppercase tracking-[.18em] text-violet-700"><WandSparkles className="h-3.5 w-3.5" />Prompt-to-template workspace</div><h1 className="mt-4 max-w-xl text-3xl font-semibold tracking-[-.06em] sm:text-5xl">Make the first draft feel less like a blank page.</h1><p className="mt-4 max-w-xl text-sm leading-6 text-slate-500 sm:text-base">Describe the landing page you want. LandMake maps supported words into safe, deterministic sections you can preview and export.</p></div><form onSubmit={submit} className="rounded-2xl border border-violet-100 bg-[#fbfaff] p-4"><label className="text-xs font-bold uppercase tracking-[.14em] text-slate-500" htmlFor="project-name">Project name</label><Input id="project-name" value={name} onChange={event => setName(event.target.value)} className="mt-2 border-violet-100 bg-white" required /><label className="mt-4 block text-xs font-bold uppercase tracking-[.14em] text-slate-500" htmlFor="landing-prompt">Your brief</label><Textarea id="landing-prompt" value={prompt} onChange={event => setPrompt(event.target.value)} className="mt-2 min-h-28 resize-none border-violet-100 bg-white" required /><Button type="submit" disabled={createProject.isPending} className="mt-4 h-11 w-full rounded-xl bg-violet-600 font-semibold text-white hover:bg-violet-700"><Plus className="mr-2 h-4 w-4" />{createProject.isPending ? "Generating…" : "Generate first draft"}</Button><p className="mt-3 text-center text-[11px] text-slate-400">No external AI call. Your brief is mapped through fixed templates.</p></form></div></section><section className="grid gap-4 md:grid-cols-3"><Card className="border-violet-100 bg-white"><CardContent className="flex items-center justify-between p-5"><div><p className="text-sm text-slate-500">Saved projects</p><p className="mt-2 text-2xl font-semibold">{isLoading ? "—" : data?.projects.length ?? 0}</p></div><span className="grid h-10 w-10 place-items-center rounded-xl bg-violet-50 text-violet-700"><FileCode2 className="h-5 w-5" /></span></CardContent></Card><Card className="border-violet-100 bg-white"><CardContent className="flex items-center justify-between p-5"><div><p className="text-sm text-slate-500">Recent activity</p><p className="mt-2 text-2xl font-semibold">{isLoading ? "—" : data?.activities.length ?? 0}</p></div><span className="grid h-10 w-10 place-items-center rounded-xl bg-amber-50 text-amber-700"><Clock3 className="h-5 w-5" /></span></CardContent></Card><Card className="border-violet-100 bg-white"><CardContent className="flex items-center justify-between p-5"><div><p className="text-sm text-slate-500">Export format</p><p className="mt-2 text-2xl font-semibold">HTML</p></div><span className="grid h-10 w-10 place-items-center rounded-xl bg-emerald-50 text-emerald-700"><Download className="h-5 w-5" /></span></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1.2fr_.8fr]"><Card className="border-violet-100 bg-white"><CardHeader className="border-b border-slate-100"><CardTitle className="tracking-[-.03em]">Your landing pages</CardTitle><CardDescription>Open a saved project to preview, regenerate, or export it.</CardDescription></CardHeader><CardContent className="p-0">{isLoading ? <div className="p-6 text-sm text-slate-500">Loading projects…</div> : data?.projects.length ? <div className="divide-y divide-slate-100">{data.projects.map(project => <button key={project.id} onClick={() => setLocation(`/projects/${project.id}`)} className="group flex w-full items-center gap-4 px-5 py-4 text-left hover:bg-violet-50/50"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-slate-950 text-white"><WandSparkles className="h-4 w-4" /></span><span className="min-w-0 flex-1"><span className="block truncate text-sm font-semibold text-slate-900">{project.name}</span><span className="mt-1 block truncate text-xs text-slate-500">{project.prompt}</span></span><ArrowUpRight className="h-4 w-4 text-slate-300 group-hover:text-violet-700" /></button>)}</div> : <div className="grid min-h-48 place-items-center p-8 text-center"><div><span className="mx-auto grid h-12 w-12 place-items-center rounded-2xl bg-violet-50 text-violet-500"><FileCode2 className="h-5 w-5" /></span><p className="mt-4 text-sm font-semibold text-slate-800">No landing pages yet</p><p className="mt-1 text-sm text-slate-500">Use the generator above to create your first draft.</p></div></div>}</CardContent></Card><Card className="border-violet-100 bg-white"><CardHeader><CardTitle className="tracking-[-.03em]">Recent activity</CardTitle></CardHeader><CardContent className="space-y-4">{data?.activities.length ? data.activities.map(activity => <div key={activity.id} className="flex gap-3"><span className="mt-1.5 h-2 w-2 shrink-0 rounded-full bg-violet-500" /><div><p className="text-sm leading-5 text-slate-700">{activity.message}</p><p className="mt-1 text-xs text-slate-400">{activity.projectName}</p></div></div>) : <p className="text-sm leading-6 text-slate-500">Your generation history will appear here.</p>}</CardContent></Card></section></div>;
}
